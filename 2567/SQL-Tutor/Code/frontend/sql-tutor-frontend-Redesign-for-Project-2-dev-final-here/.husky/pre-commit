echo '🏗️👷 วีรภัทรกำลังแอบดูโค้ดของคุณ 🚀🔨'

npm run format ||
(
  echo '😱 โอ้ววว! เขียนโค้ดได้แย่มาก - Prettier Check Failed. 😱'
  echo 'อย่างน้อยๆก็เขียนให้เป็นมาตรฐานหน่อย. ลองรัน "npm run format" เพื่อตรวจเช็คดูนะ.';
  false;
)

npm run lint ||
(
  echo '😬 โอ้ววว! โค้ดโคตรกากกกกกกก - ESLint Check Failed. 😬'
  echo 'โค้ดของคุณมีปัญหาบางอย่าง. ลองรัน "npm run lint" เพื่อแก้ไขและลอง commit อีกครั้ง.';
  false;
)

echo '🤔🤔🤔🤔 ...โอเค โค้ดดูดีอยู่นะ... 🤔🤔🤔🤔'
echo 'ทีนี้มาลอง build code กันดูบ้าง'
echo '👷🔨👷🔨 ...กำลังทดสอบสร้างไฟล์สำหรับ production... 👷🔨👷🔨'

echo '✅✅✅✅ สำเร็จ! ง่ายๆแค่นี้เอง. commit ให้แล้วจ้า... ✅✅✅✅'