services:
  backend:
    container_name: test-backend
    build:
      context: .
      dockerfile: Dockerfile
    port:
      "5000:5000"
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "1.0"
          memory: "512M"